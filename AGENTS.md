# AGENTS.md

本仓库用于实现“供应商整板整车到货排程优化算法”。本文档约束并指导后续 Agent 在本仓库内编写、测试和提交代码。

## 项目目标

使用 Python 3.11 与 OR-Tools CP-SAT 实现供应商送货车辆排程优化：

- 输入加工计划、供应商拆分结果、包规、车规和可到货时间规则。
- 将任务令的物料需求拆分到小时级消耗桶。
- 每个小时消耗桶必须在其开工小时前的可配置窗口内到达，默认窗口为 `T-8` 到 `T-3`。
- 车辆只允许装同一家供应商的物料，不跨供应商拼车。
- 所有物料按整栈板装车，需求数量不足整板时按包规向上取整。
- 在满足时效、包规、车规和可到货时间约束的前提下，优先最小化出车数。

## 业务术语

- **任务令**：产线上批量加工一个物料编码的时间段，包含任务令、线体、物料编码、计划量、休息时间、开工时间和完工时间。
- **实际加工时长**：`完工时间 - 开工时间 - 休息时间`，单位小时。
- **小时消耗量**：`计划量 / 实际加工时长`，需要按小时级时间桶生成需求。
- **包规**：一个栈板包含的物料数量。所有物料按整板交付，板数为 `ceil(数量 / 包规)`。
- **车规**：某供应商单车可装载的最大栈板数。
- **不可到货时间**：默认每天小时 `1, 2, 7, 12, 13, 18` 不允许到货，应作为配置项。

## 输入数据约定

实现时应将业务输入清洗为小时级数据，推荐保留以下逻辑字段。

### 加工计划表

必需字段：

- `line`：线体
- `mfg_order`：任务令
- `item_code`：原材料编码
- `plan_qty`：计划量
- `break_hours`：休息时间，单位小时
- `start_time`：开工时间
- `end_time`：完工时间

约束与处理：

- 一个任务令只对应一个物料编码；一个物料编码可对应多个任务令。
- 实际加工时长必须大于 0。
- 小时消耗拆分后，同一任务令的需求数量汇总必须等于原计划量。
- 生成小时级需求时，应避免浮点累计误差；涉及整数数量时，最后一个时间桶可吸收舍入差额。

### 拆分结果表

必需字段：

- `item_code`：物料编码
- `supplier`：供应商名称
- `location`：货位
- `demand_qty`：需求数量
- `demand_id`：需求 ID

约束与处理：

- 拆分结果汇总后应与加工计划总需求一致。
- 输出结果中，同一 `demand_id` 的满足数量汇总必须等于该需求 ID 的需求数量。

### 包规基表

必需字段：

- `item_code`：物料编码
- `pack_size`：包规

约束与处理：

- `pack_size` 必须为正整数。
- 板数计算使用向上取整。

### 车规表

必需字段：

- `supplier`：供应商名称
- `truck_capacity_pallets`：车规，单位板/车

约束与处理：

- `truck_capacity_pallets` 必须为正整数。
- 每辆车只能服务一个供应商。

## 输出数据约定

输出表至少包含：

- `supplier`：供应商
- `truck_no`：车次
- `item_code`：物料编码
- `mfg_order`：任务令
- `line`：线体
- `location`：货位
- `demand_id`：需求 ID
- `fulfilled_qty`：满足数量
- `pack_size`：包规
- `pallets`：该行板数
- `total_pallets`：该车总板数
- `truck_capacity_pallets`：车规
- `load_rate`：装载率，`total_pallets / truck_capacity_pallets`
- `planned_arrival_time`：计划到货时间

必须保证：

- 同一任务令的 `fulfilled_qty` 汇总等于其计划需求数量。
- 同一需求 ID 的 `fulfilled_qty` 汇总等于其拆分需求数量。
- 同一车次的 `total_pallets` 不超过对应供应商车规。
- 同一车次只出现一个供应商。
- 到货时间不落在不可到货小时。
- 每个被满足的小时消耗桶，到货时间位于其允许窗口内。

## 优化建模指导

- 求解器使用 OR-Tools CP-SAT。
- 时间粒度以小时为基本单位，输入中的分钟秒需要按业务规则清洗到小时桶。
- 推荐先按供应商分组建模或分解求解，因为车辆不可跨供应商混装。
- 主目标：最小化总出车数。
- 次级目标可按业务需要逐步加入，例如：
  - 最大化车辆装载率或最小化剩余空板位。
  - 尽量推迟到货以降低现场占用。
  - 减少同一任务令或同一需求 ID 的拆分次数。
- 若使用多目标，应采用 CP-SAT 的加权目标或分阶段求解，并在代码注释中说明权重含义。
- 所有配置项（提前到货窗口、不可到货小时、求解时限、最优性 gap 等）应集中管理，不要硬编码在求解逻辑深处。

## 代码组织建议

后续创建代码时，优先采用如下结构：

```text
src/
  manufacture_jis/
    __init__.py
    config.py          # 默认配置与参数校验
    schemas.py         # 输入/输出数据结构
    preprocessing.py   # 时间清洗、小时需求拆分、包规/车规关联
    optimizer.py       # CP-SAT 建模与求解
    postprocessing.py  # 输出表展开、装载率计算、结果校验
    validation.py      # 输入与输出一致性校验
tests/
  test_preprocessing.py
  test_optimizer.py
  test_validation.py
```

如实际项目已有不同结构，应遵循既有结构，不要为符合上述建议而进行无必要的大规模重构。

## Python 与依赖规范

- 使用 Python 3.11。
- 求解器依赖使用 `ortools`。
- 推荐使用 `pandas` 处理表格输入输出，但核心优化模型应尽量与具体文件格式解耦。
- 新增公共函数应提供类型标注。
- 对业务约束、目标函数权重和时间清洗规则，应写清楚注释或 docstring。
- 不要在 import 外层添加 try/except。

## 测试要求

新增或修改算法逻辑时，应补充自动化测试，至少覆盖：

- 小时级消耗拆分数量守恒。
- 包规向上取整。
- 不可到货时间过滤。
- 到货窗口 `T-8` 到 `T-3` 约束。
- 单供应商单车车规容量约束。
- 多供应商不拼车。
- 输出中任务令与需求 ID 的数量守恒。
- 一个小型可手算案例，验证最小出车数。

提交前优先运行：

```bash
python -m pytest
```

如果项目尚未配置测试框架，至少运行能覆盖新增代码的最小脚本或单元测试，并在最终回复中说明限制。

## 数据与数值处理注意事项

- 业务数量若本质为整数，应在内部使用整数表达，避免浮点误差影响 CP-SAT。
- CP-SAT 不支持浮点变量；目标函数和约束中的比例、权重应缩放为整数。
- 时间建议转换为离散小时索引，并保留索引到真实 `datetime` 的映射。
- 对不能满足时效窗口或容量约束的输入，应返回清晰的不可行原因或诊断信息。

## 提交与 PR 说明

- 每次代码变更后检查 `git status`，只提交与当前任务相关的文件。
- 提交信息使用简洁英文祈使句，例如 `Add optimization project guidance`。
- PR 描述应包含：
  - Summary：本次变更要点。
  - Testing：已运行的测试或检查。
  - Notes/Risks：如有，说明尚未实现或存在的业务假设。
